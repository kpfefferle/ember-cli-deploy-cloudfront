machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
  node:
    version: "7.10.0"
dependencies:
  override:
    - yarn install --no-lockfile
  cache_directories:
    - ~/.cache/yarn
test:
  override:
    - yarn test
