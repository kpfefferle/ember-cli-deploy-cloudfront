machine:
  node:
    version: "stable"
dependencies:
  pre:
    - npm install -g yarn
    - npm install -g bower
    - sudo apt-get update; sudo apt-get install -y --only-upgrade google-chrome-stable
  override:
    - yarn install
  post:
    - bower install
